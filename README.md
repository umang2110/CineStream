# CineStream

Next.js movie recommendation app.

## Setup

1. Install dependencies:
   - `npm install`
2. Create `.env.local` with required keys (do not commit it).
3. Run dev server:
   - `npm run dev`

## Build

- `npm run build`
